version: 2
updates:
  # Keeps the SHA pins current: Dependabot rewrites both the SHA and the
  # version comment, so pinning does not mean going stale unnoticed.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
